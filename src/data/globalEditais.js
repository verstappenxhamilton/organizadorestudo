
import parsedData from './parsedEditais.json';

// Use the parsed data directly.
// We might want to enrich it or validate it, but for now we trust the parser.
export const GLOBAL_EDITAIS = parsedData;
